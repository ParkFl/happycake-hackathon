# Bot wrappers — Python dependencies
# Keep this minimal; agent extends as needed.

python-telegram-bot==21.4
fastapi==0.115.0
uvicorn[standard]==0.30.0
httpx==0.27.0
python-dotenv==1.0.1
pydantic>=2.9,<3.0    # 2.8 has a ForwardRef bug with FastAPI 0.115; 2.9+ fixed it.
apscheduler==3.10.4
